# URL of the deployed backend API (no trailing slash)
NEXT_PUBLIC_API_URL="http://localhost:3001"

# Secret for signing the session cookie that stores the JWT
# Must match nothing on the backend – it's only used by Next.js middleware
AUTH_SECRET="change-me-in-production-use-a-long-random-string"
